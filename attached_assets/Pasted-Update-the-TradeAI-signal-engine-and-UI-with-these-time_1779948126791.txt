Update the TradeAI signal engine and UI with these timeframe changes:

=== PRIMARY SIGNAL TIMEFRAME ===
The main AI signal on dashboard and signal cards must be based on 1D (daily) candles.
This is the PRIMARY call shown prominently.

=== ALL SUPPORTED TIMEFRAMES ===
The platform must show signals for ALL these timeframes:
- 1D (Daily) — PRIMARY, shown by default, largest display
- 1W (Weekly) — shown as secondary signal
- 1h (1 Hour) — shown as short-term signal  
- 15m (15 Minute) — shown as intraday signal
- 5m (5 Minute) — shown as scalping signal
- 1m (1 Minute) — shown as scalping signal

=== DASHBOARD SIGNAL CARDS — UPDATE ===
Each signal card must now show:
- Stock symbol + current price
- PRIMARY SIGNAL (1D): big BUY/SELL/HOLD badge
- Pattern detected on daily chart (e.g. "Daily Hammer", "Weekly Engulfing")
- Entry / Target / SL based on daily candle
- Confidence % 
- Below that, a MULTI-TIMEFRAME CONSENSUS row:
  1D | 1W | 1h | 15m | 5m | 1m
  Each showing a small BUY/SELL/HOLD colored dot
  Example: 1D: BUY(green) | 1W: BUY(green) | 1h: HOLD(yellow) | 15m: SELL(red)
- If 4+ timeframes agree → show "Strong Signal" badge
- If mixed → show "Mixed Signal" badge

=== STOCK DETAIL PAGE — UPDATE ===
- Default chart timeframe = 1D when page opens
- Timeframe buttons: 1m | 5m | 15m | 1h | 1D | 1W
- For each timeframe, show the AI signal for that specific timeframe
- Signal box updates when timeframe is switched
- Show a "Timeframe Analysis" table:
  Timeframe | Signal | Pattern | Confidence
  1D        | BUY    | Hammer  | 82%
  1W        | BUY    | Engulfing | 78%
  1h        | HOLD   | Doji    | 55%
  15m       | SELL   | Shooting Star | 67%
  5m        | BUY    | Morning Star | 71%
  1m        | HOLD   | Doji    | 48%

=== SIGNALS PAGE — UPDATE ===
- Default filter = 1D signals
- Timeframe filter buttons: 1D | 1W | 1h | 15m | 5m | 1m
- Add a note under title: "Primary signals based on Daily (1D) candle analysis"

=== BACKEND UPDATE ===
In the signal generation function, generate separate signals for each timeframe per stock:
timeframes = ["1m", "5m", "15m", "1h", "1D", "1W"]

For each stock, generate a signal dict like:
{
  "symbol": "NIFTY50",
  "primary_signal": "BUY",  (always from 1D)
  "primary_pattern": "Hammer",
  "primary_confidence": 82,
  "entry": 22500,
  "target": 22680,
  "sl": 22410,
  "timeframe_signals": {
    "1D": {"signal": "BUY", "pattern": "Hammer", "confidence": 82},
    "1W": {"signal": "BUY", "pattern": "Engulfing", "confidence": 78},
    "1h": {"signal": "HOLD", "pattern": "Doji", "confidence": 55},
    "15m": {"signal": "SELL", "pattern": "Shooting Star", "confidence": 67},
    "5m": {"signal": "BUY", "pattern": "Morning Star", "confidence": 71},
    "1m": {"signal": "HOLD", "pattern": "Doji", "confidence": 48}
  },
  "consensus": "Strong BUY"  (if 4+ timeframes agree)
}

Route /data/signals returns this full structure for all stocks.
Route /data/chart/<symbol>/<timeframe> returns OHLCV for that specific timeframe.

=== IMPORTANT LABEL CHANGES ===
- On dashboard cards, below the main signal badge, add small text: "Based on Daily candle analysis"
- On signals page, add header note: "Signals generated by analysing daily candlestick patterns using CNN image processing"
- Confidence threshold for "Strong Signal": 4 or more timeframes agree = Strong, else Mixed